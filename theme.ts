import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4CAF50", // Verde
    },
    secondary: {
      main: "#FF5722", // Laranja
    },
  },
});

export default theme;
